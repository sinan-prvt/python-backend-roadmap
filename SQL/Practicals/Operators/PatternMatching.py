# =================================== Pattern Matching Operators



# --------------- LIKE Operator

# SELECT * FROM Employees
# WHERE name LIKE 'A%';    -- Retrieve employees whose names start with 'A'




# --------------- NOT LIKE Operator

# SELECT * FROM Employees
# WHERE name NOT LIKE 'A%';    -- Retrieve employees whose names do not start with




# -------------- ILIKE Operator (case-insensitive)

# SELECT * FROM Employees
# WHERE name ILIKE 'a%';    -- Retrieve employees whose names start with 'a' (case-insensitive)




# -------------- NOT ILIKE Operator (case-insensitive)

# SELECT * FROM Employees
# WHERE name NOT ILIKE 'a%';    -- Retrieve employees whose names do not start with 'a' (case-insensitive)




# ------------- WILD CARD CHARACTERS

# SELECT * FROM Employees
# WHERE name LIKE '_a%';    -- Retrieve employees whose names have 'a' as




